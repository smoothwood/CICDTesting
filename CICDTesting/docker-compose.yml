version: "3"
services:
  cicdtestingwebsite:
    image: ycmacr.azurecr.io/cicdtesting
    ports:
      - 80:80
    environment:
      - ASPNETCORE_ENVIRONMENT=Staging
      - HelloMessage=HELLO from docker.compose.yml
